export interface IEducation {
  uuid?: string;
  name: string; // Nome de uma faculdade ou Especialidade
  description?: string; // Descrição (opcional)
  from?: string; // Data de início ISO 8604
  until?: string; // Data de início ISO 8604
}
